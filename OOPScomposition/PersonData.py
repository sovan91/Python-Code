class Person:

    # roi = 4.5

    def __init__(self, id, name, age):
        self.id = id
        self.name = name
        self.age = age

    def show(self):
        print(self.id)
        print(self.name)
        print(self.age)

